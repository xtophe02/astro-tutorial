---
const pageTitle = "About Me";
const identity = {
  firstName: "Sarah",
  country: "Canada",
  occupation: "Technical Writer",
  hobbies: ["photography", "birdwatching", "baseball"],
};

const skills = ["HTML", "CSS", "JavaScript", "React", "Astro", "Writing Docs"];

// Initial state values
const happy = true;
const finished = false;
const goal = 3;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Astro</title>
    <title>{pageTitle}</title>
  </head>
  <body>
    <a href="/">Home</a>
    <a href="/about/">About</a>
    <a href="/blog/">Blog</a>
    <h1>{pageTitle}</h1>
    <h2>... and my new Astro site!</h2>

    <p>I am working through Astro's introductory tutorial. This is the second page on my website, and it's the first one I built myself!</p>

    <p>This site will update as I complete more of the tutorial, so keep checking back and see how my journey is going!</p>
    <p>Here are a few facts about me:</p>
<ul>
  <li>My name is {identity.firstName}.</li>
  <li>I live in {identity.country} and I work as a {identity.occupation}.</li>
  {identity.hobbies.length >= 2 &&
    <li>Two of my hobbies are: {identity.hobbies[0]} and {identity.hobbies[1]}</li>
  }
</ul>
<p>My skills are:</p>
<ul>
  {skills.map((skill) => <li>{skill}</li>)}
</ul>

<div id="happy-button">
  <button id="toggle-happy">I am happy</button>
</div>

{finished && <p>I finished this tutorial!</p>}

{goal === 3 ? <p>My goal is to finish in 3 days.</p> : <p>My goal is not 3 days.</p>}

<div id="finished-button">
  <button id="toggle-finished">I did not finish this tutorial!</button>
</div>

<script>
  // Client-side JavaScript
  let isHappy = true;
  let isFinished = false;
  
  // Toggle happy state
  document.getElementById('toggle-happy')?.addEventListener('click', () => {
    isHappy = !isHappy;
    const button = document.getElementById('toggle-happy');
    if (button) {
      button.textContent = isHappy ? "I am happy" : "I am not happy";
    }
  });
  
  // Toggle finished state
  document.getElementById('toggle-finished')?.addEventListener('click', () => {
    isFinished = !isFinished;
    const button = document.getElementById('toggle-finished');
    if (button) {
      button.textContent = isFinished ? "I finished this tutorial!" : "I did not finish this tutorial!";
    }
    
    // Toggle the finished message
    const finishedMessage = document.getElementById('finished-message');
    if (isFinished) {
      if (!finishedMessage) {
        const p = document.createElement('p');
        p.id = 'finished-message';
        p.textContent = 'I finished this tutorial!';
        document.getElementById('finished-button')?.after(p);
      }
    } else {
      finishedMessage?.remove();
    }
  });
</script>

  </body>
</html>

